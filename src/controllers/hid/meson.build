if has_hid
mixxx_sources += files([ 
    'hidcontroller.cpp',
    'hidenumerator.cpp',
])

mixxx_headers += files([ 
    'hidcontroller.h',
])

endif

if has_hid or has_bulk
mixxx_sources += files([ 
    'hidcontrollerpresetfilehandler.cpp',
])
endif
